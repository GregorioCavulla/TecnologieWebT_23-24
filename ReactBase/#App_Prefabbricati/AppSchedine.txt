 
// App.js
import React, { useState } from 'react';
import Compilazione from './Compilazione';
import Visualizzazione from './Visualizzazione';
import Estrazione from './Estrazione';

const App = () => {
  const [schedina, setSchedina] = useState([]);
  const [risultato, setRisultato] = useState('');

  const handleCompila = (numeri) => {
    setSchedina(numeri);
    setRisultato('');
  };

  const handleEstrai = (numeriEstratti) => {
    const risultato = verificaRisultato(numeriEstratti, schedina);
    setRisultato(risultato);
  };

  const verificaRisultato = (numeriEstratti, schedina) => {
    // Implementa la logica per verificare il risultato
    // e restituire 'ambo', 'terno', 'quaterna', 'cinquina' o ''
    // in base al risultato dell'estrazione
  };

  return (
    <div className="app">
      <Compilazione onCompila={handleCompila} />
      <Visualizzazione numeri={schedina} risultato={risultato} />
      <Estrazione onEstrai={handleEstrai} />
    </div>
  );
};

export default App;
